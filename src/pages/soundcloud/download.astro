---
import {taskQueue} from "@src/utils/task-queue"; 

const eventUrl = Astro.url.searchParams.get('event-url');
// add item to the database

// add item to the background task quue with the database id 

// htmx should include a thing to reload every few seconds until it is done
---
